pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name ecinstancetcbdemo --template-body file://instance.yaml --tags Key=Name,Value=114poc01 --region 'us-west-2'"
              }
             }
            }
            }
